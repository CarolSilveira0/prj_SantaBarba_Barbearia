from django.apps import AppConfig

# onde indicamos o nome dos nossos apps usados no site
class AppBarbeariaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_barbearia'
